a(ng-attr-href="/products/{{ product.id }}", flex, layout="column")
    md-card(flex, layout="column")
        .img-container
            img.md-card-image(ng-src="{{ product | productImage }}" alt="Product Logo")
        md-card-title: md-card-title-text
            .md-headline(ng-bind=":: product.name")
            .md-subheadline(ng-bind=":: product.description")
        md-card-content(layout="row")
            div(flex, ng-bind=":: product.price | currency:'₪'")
            div.out-of-stock(ng-if=":: !product.amount") Out of stock!